export const myPush = (Array, element) => {
    const returnArray = [];
    for (let i = 0; i < array.length; i++) {
        returnArray[i] = array[i];
        
    }
    returnArray[arr.length] = element;
    return returnArray;
}
export const myPop = (Array, element){
    
}